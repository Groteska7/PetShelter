<Window x:Class="PetShelter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PetShelter - Приют для животных" 
        Height="450" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filters GroupBox -->
        <GroupBox Grid.Row="0" Header="Фильтры" FontSize="14" FontWeight="Bold" Margin="0,0,0,15">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Shelter -->
                <Label Grid.Row="0" Grid.Column="0" Content="Приют:" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="0" Grid.Column="1" x:Name="CboShelter" Margin="5" Height="25"/>

                <!-- Pet Type -->
                <Label Grid.Row="1" Grid.Column="0" Content="Вид животного:" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CboPetType" Margin="5" Height="25"/>

                <!-- Open Area Only -->
                <CheckBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                         x:Name="ChkOpenAreaOnly" Content="Только приюты с открытой территорией" 
                         Margin="5,10,5,5"/>
            </Grid>
        </GroupBox>

        <!-- Report Format GroupBox -->
        <GroupBox Grid.Row="1" Header="Формат отчета" FontSize="14" FontWeight="Bold" Margin="0,0,0,15">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Формат:" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" x:Name="CboFormat" Margin="5" Height="25"/>
            </Grid>
        </GroupBox>

        <!-- Show Pets Button -->
        <Button Grid.Row="2" Content="Показать питомцев" 
                FontSize="14" FontWeight="Bold" 
                Padding="20,10" Margin="0,10"
                Click="BtnShowPets_Click"/>
    </Grid>
</Window>